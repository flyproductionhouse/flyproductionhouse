import { ReplaceExtensionPipe } from './replace-extension.pipe';

describe('ReplaceExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
